## 0.0.2

- **Breaking**: `ContextualBarItem` now uses `selectedWidget` / `unselectedWidget`.
- Add `disableMaterialInkEffects` to remove Material splash/highlight inside the bar.

## 0.0.1

- Initial release.
